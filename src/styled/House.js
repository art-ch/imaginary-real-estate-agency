import styled from 'styled-components';

const HouseWrapper = styled.main`
  margin: 0 auto;
  .container {
    max-width: 1000px;
  }

  .btn-container {
    .house-btn {
      width: 140px;
    }
    .back-btn {
      width: 288px;
    }
  }
`;

export default HouseWrapper;
